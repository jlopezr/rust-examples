# Calling a Rust library from C

## Build 

An additional step is required to build the C main program:

    cargo build
    cd src
    make

## Usage

Open the terminal and type:

    cd src
    ./main
